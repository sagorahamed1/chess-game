import 'package:flutter/material.dart';

class PlayComputerScreen extends StatelessWidget {
  const PlayComputerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
